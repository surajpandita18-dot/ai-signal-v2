{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "/opt/homebrew/bin/jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; case \"$f\" in *src/components/article/*|*src/app/issue/*|*src/components/SiteNav*|*src/components/SiteFooter*|*src/app/globals.css|*src/lib/email-template.ts|*src/lib/deep-dive-email.ts) printf '{\"systemMessage\":\"Design surface edited (%s). Before claiming done: run `npm run audit:design` and verify with the aisignal-design-critic agent on 1200/390 screenshots. Body >=15px, lime functional only, no eyebrow stack/card-in-card/SaaS patterns.\"}' \"$f\";; esac; } 2>/dev/null"
          }
        ]
      }
    ]
  }
}
